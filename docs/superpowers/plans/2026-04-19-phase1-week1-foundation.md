# vipsOS Phase 1 — Week 1: Foundation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Scaffold `apps/web` with the full stack, build the App Shell (sidebar + topbar + routing), and create all typed stub data files + Pinia stores.

**Architecture:** Standalone Vite + Vue 3 app in `apps/web/`. All data lives in typed TypeScript stubs (`src/data/*.ts`) imported by Pinia stores — no API calls, no backend. The App Shell provides a persistent sidebar + topbar layout; Vue Router serves 6 routes with placeholder views. Replacing stub data with real API calls later requires only changing the store, not any component.

**Tech Stack:** Vue 3.5, TypeScript 5, Vite 6, Pinia 2, Vue Router 4, Shadcn/Vue (New York style), Tailwind CSS 4, Lucide Vue Next, Vitest 2, @vue/test-utils

---

## File Map

All files are new. No existing files are modified.

```
apps/web/
├── index.html
├── vite.config.ts                         # Vite + Tailwind v4 + vitest config
├── tsconfig.json
├── tsconfig.app.json
├── tsconfig.node.json
├── package.json
├── components.json                        # Created by shadcn-vue init
└── src/
    ├── main.ts                            # App bootstrap: Pinia + Router + mount
    ├── App.vue                            # Root: renders AppShell only
    ├── style.css                          # Tailwind import + CSS design tokens
    ├── types/
    │   └── index.ts                       # All shared TypeScript types
    ├── router/
    │   └── index.ts                       # Vue Router: 6 routes, lazy-loaded views
    ├── data/
    │   ├── workflows.ts                   # WorkflowSummary[] + WorkflowDefinition[]
    │   ├── connectors.ts                  # ConnectorCard[] + ConnectorDetail[]
    │   ├── runs.ts                        # RunRecord[] + RunDetail[]
    │   └── dashboard.ts                   # DashboardStats (references runs.ts)
    ├── stores/
    │   ├── workflows.ts                   # useWorkflowsStore — reads from data/workflows
    │   ├── connectors.ts                  # useConnectorsStore — reads from data/connectors
    │   ├── runs.ts                        # useRunsStore — reads from data/runs
    │   └── __tests__/
    │       ├── workflows.test.ts
    │       ├── connectors.test.ts
    │       └── runs.test.ts
    ├── views/
    │   ├── DashboardView.vue              # Placeholder
    │   ├── WorkflowsView.vue              # Placeholder
    │   ├── WorkflowBuilderView.vue        # Placeholder
    │   ├── ConnectorsView.vue             # Placeholder
    │   ├── ConnectorDetailView.vue        # Placeholder
    │   └── RunDetailView.vue              # Placeholder
    └── components/
        └── layout/
            ├── AppShell.vue               # Outer layout: sidebar + topbar + router-view
            ├── AppSidebar.vue             # Left nav with 5 items + active state
            ├── AppTopBar.vue              # Top bar: workspace switcher + user/notif buttons
            └── __tests__/
                └── AppSidebar.test.ts
```

---

## Task 1: Scaffold the project

**Files:**
- Create: `apps/web/` (entire directory, generated by Vite)

- [ ] **Step 1: Run create-vite from the repo root**

```bash
cd /Users/leandroarruda/projects/vipsOS
mkdir -p apps
npm create vite@latest apps/web -- --template vue-ts
```

Expected output ends with: `Done. Now run: cd apps/web && npm install`

- [ ] **Step 2: Install dependencies**

```bash
cd apps/web && npm install
```

- [ ] **Step 3: Verify the dev server starts**

```bash
npm run dev
```

Expected: `VITE v6.x ready in X ms` and `➜ Local: http://localhost:5173/`

Open http://localhost:5173 in a browser. You should see the default Vite + Vue welcome page.

- [ ] **Step 4: Stop the dev server (Ctrl+C)**

- [ ] **Step 5: Remove boilerplate files**

```bash
rm -f src/components/HelloWorld.vue
rm -f src/assets/vue.svg
rm -f public/vite.svg
rm -f src/style.css
```

- [ ] **Step 6: Replace App.vue with a blank placeholder**

`src/App.vue`:
```vue
<template>
  <div>loading...</div>
</template>
```

- [ ] **Step 7: Create a blank style.css**

`src/style.css`:
```css
/* populated in Task 2 */
```

- [ ] **Step 8: Update index.html title**

In `index.html`, replace:
```html
<title>Vite + Vue</title>
```
with:
```html
<title>vipsOS</title>
```

- [ ] **Step 9: Commit**

```bash
git add apps/web
git commit -m "feat: scaffold apps/web with Vite + Vue 3 + TypeScript"
```

---

## Task 2: Set up Shadcn/Vue and Tailwind CSS v4

**Files:**
- Modify: `apps/web/vite.config.ts`
- Modify: `apps/web/src/style.css`
- Modify: `apps/web/tsconfig.app.json`
- Create: `apps/web/components.json` (by shadcn-vue init)

- [ ] **Step 1: Install Tailwind CSS v4 and the Vite plugin**

```bash
cd apps/web
npm install -D tailwindcss @tailwindcss/vite
```

- [ ] **Step 2: Update vite.config.ts to use Tailwind v4 and the @ alias**

`apps/web/vite.config.ts`:
```typescript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import tailwindcss from '@tailwindcss/vite'
import { fileURLToPath, URL } from 'node:url'

export default defineConfig({
  plugins: [vue(), tailwindcss()],
  resolve: {
    alias: {
      '@': fileURLToPath(new URL('./src', import.meta.url)),
    },
  },
})
```

- [ ] **Step 3: Add Tailwind import to style.css**

`apps/web/src/style.css`:
```css
@import "tailwindcss";
```

- [ ] **Step 4: Verify Tailwind works**

`apps/web/src/App.vue`:
```vue
<template>
  <div class="p-8 text-2xl font-bold text-blue-600">Tailwind works</div>
</template>
```

Run `npm run dev` and open http://localhost:5173. You should see bold blue text. Stop the server.

- [ ] **Step 5: Run Shadcn/Vue init**

```bash
npx shadcn-vue@latest init
```

When prompted, make these choices:
- **TypeScript**: Yes
- **Framework**: Vite
- **Style**: New York
- **Base color**: Zinc
- **Global CSS file**: `src/style.css`
- **CSS variables for colors**: Yes
- **Components directory**: `src/components/ui`
- **Utils path**: `src/lib/utils`

This creates `components.json`, updates `src/style.css` with CSS variable definitions, and installs `class-variance-authority`, `clsx`, `tailwind-merge`, `radix-vue`, and `lucide-vue-next`.

- [ ] **Step 6: Add custom design system tokens to style.css**

Open `src/style.css` and append the following after the `:root` and `.dark` blocks that shadcn-vue created:

```css
@layer base {
  :root {
    --success: 142 76% 36%;
    --success-foreground: 0 0% 98%;
    --warning: 38 92% 50%;
    --warning-foreground: 0 0% 3.9%;
    --danger: 0 84% 60%;
    --danger-foreground: 0 0% 98%;
    --info: 217 91% 60%;
    --info-foreground: 0 0% 3.9%;
  }
  .dark {
    --success: 142 70% 45%;
    --warning: 38 85% 55%;
    --danger: 0 72% 51%;
    --info: 217 85% 65%;
  }
}
```

- [ ] **Step 7: Update tsconfig.app.json to include path alias**

Open `tsconfig.app.json`. Ensure it has:
```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
```

If `compilerOptions` already exists, merge these two keys in. Do not remove existing keys.

- [ ] **Step 8: Verify the setup compiles**

```bash
npm run build
```

Expected: build succeeds with no TypeScript errors.

- [ ] **Step 9: Commit**

```bash
git add apps/web
git commit -m "feat: add Tailwind CSS v4 and Shadcn/Vue to apps/web"
```

---

## Task 3: Configure Vitest

**Files:**
- Modify: `apps/web/vite.config.ts`

- [ ] **Step 1: Install vitest, jsdom, and @vue/test-utils**

```bash
cd apps/web
npm install -D vitest jsdom @vue/test-utils
```

- [ ] **Step 2: Update vite.config.ts to include vitest config**

`apps/web/vite.config.ts`:
```typescript
/// <reference types="vitest" />
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import tailwindcss from '@tailwindcss/vite'
import { fileURLToPath, URL } from 'node:url'

export default defineConfig({
  plugins: [vue(), tailwindcss()],
  resolve: {
    alias: {
      '@': fileURLToPath(new URL('./src', import.meta.url)),
    },
  },
  test: {
    environment: 'jsdom',
    globals: false,
  },
})
```

- [ ] **Step 3: Add test scripts to package.json**

Open `apps/web/package.json` and add to the `"scripts"` block:
```json
"test": "vitest run",
"test:watch": "vitest"
```

The scripts block should look like:
```json
"scripts": {
  "dev": "vite",
  "build": "vue-tsc -b && vite build",
  "preview": "vite preview",
  "test": "vitest run",
  "test:watch": "vitest"
}
```

- [ ] **Step 4: Write a smoke test to verify vitest works**

Create `apps/web/src/vitest-smoke.test.ts`:
```typescript
import { describe, it, expect } from 'vitest'

describe('vitest setup', () => {
  it('runs', () => {
    expect(1 + 1).toBe(2)
  })
})
```

- [ ] **Step 5: Run tests**

```bash
npm test
```

Expected output:
```
✓ src/vitest-smoke.test.ts (1)
Test Files  1 passed (1)
Tests       1 passed (1)
```

- [ ] **Step 6: Delete the smoke test**

```bash
rm src/vitest-smoke.test.ts
```

- [ ] **Step 7: Commit**

```bash
git add apps/web
git commit -m "feat: add Vitest for unit testing in apps/web"
```

---

## Task 4: Install remaining dependencies

**Files:**
- Modify: `apps/web/package.json` (updated by npm install)

- [ ] **Step 1: Install Pinia and Vue Router**

```bash
cd apps/web
npm install pinia vue-router
```

- [ ] **Step 2: Install Vue Flow**

```bash
npm install @vue-flow/core @vue-flow/controls @vue-flow/minimap
```

- [ ] **Step 3: Verify TypeScript can see the new packages**

```bash
npm run build
```

Expected: build succeeds with no errors.

- [ ] **Step 4: Commit**

```bash
git add apps/web/package.json apps/web/package-lock.json
git commit -m "feat: install Pinia, Vue Router, and Vue Flow"
```

---

## Task 5: Define shared TypeScript types

**Files:**
- Create: `apps/web/src/types/index.ts`

- [ ] **Step 1: Create the types file**

`apps/web/src/types/index.ts`:
```typescript
export type WorkflowStatus = 'draft' | 'published' | 'archived'
export type RunStatus = 'pending' | 'running' | 'success' | 'failed' | 'cancelled'
export type NodeStatus = 'pending' | 'running' | 'success' | 'failed' | 'skipped'
export type TriggerType = 'manual' | 'schedule' | 'webhook'
export type NodeType =
  | 'connector.source'
  | 'connector.destination'
  | 'transform.map'
  | 'logic.branch'
  | 'trigger'
export type ConnectorCategory = 'database' | 'saas' | 'storage' | 'messaging' | 'analytics'
export type AuthMethod = 'oauth2' | 'api-key' | 'basic' | 'none'
export type LogLevel = 'info' | 'warn' | 'error'

export interface WorkflowTrigger {
  type: TriggerType
  cron?: string
  webhookUrl?: string
}

export interface WorkflowNode {
  id: string
  type: NodeType
  label: string
  config: Record<string, unknown>
  connectorId?: string
}

export interface WorkflowEdge {
  id: string
  source: string
  target: string
}

export interface WorkflowDefinition {
  workflowId: string
  version: number
  status: WorkflowStatus
  name: string
  description?: string
  trigger: WorkflowTrigger
  nodes: WorkflowNode[]
  edges: WorkflowEdge[]
}

export interface WorkflowSummary {
  workflowId: string
  name: string
  status: WorkflowStatus
  lastRunAt?: string
  lastRunStatus?: RunStatus
  updatedAt: string
  trigger: WorkflowTrigger
}

export interface ConnectorCard {
  connectorId: string
  name: string
  description: string
  category: ConnectorCategory
  authMethod: AuthMethod
  logoInitial: string
  certified: boolean
  installed: boolean
}

export interface ConnectorAction {
  actionId: string
  name: string
  description: string
}

export interface ConnectorDetail extends ConnectorCard {
  version: string
  actions: ConnectorAction[]
  authRequirements: string[]
  docsUrl?: string
}

export interface NodeLogEntry {
  timestamp: string
  level: LogLevel
  message: string
}

export interface NodeRunDetail {
  nodeId: string
  nodeLabel: string
  status: NodeStatus
  startedAt?: string
  completedAt?: string
  durationMs?: number
  logs: NodeLogEntry[]
}

export interface RunRecord {
  runId: string
  workflowId: string
  workflowName: string
  status: RunStatus
  triggeredBy: 'manual' | 'schedule' | 'webhook'
  startedAt: string
  completedAt?: string
  durationMs?: number
  nodeCount: number
  failedNodeCount: number
}

export interface RunDetail extends RunRecord {
  nodes: NodeRunDetail[]
}

export interface DashboardStats {
  totalWorkflows: number
  activeWorkflows: number
  totalRunsToday: number
  failedRunsToday: number
  successRate: number
  connectorCount: number
  recentRuns: RunRecord[]
  failedRuns: RunRecord[]
}
```

- [ ] **Step 2: Verify types compile**

```bash
cd apps/web && npm run build
```

Expected: build succeeds.

- [ ] **Step 3: Commit**

```bash
git add apps/web/src/types
git commit -m "feat: define shared TypeScript types for vipsOS domain"
```

---

## Task 6: Create stub data files

**Files:**
- Create: `apps/web/src/data/workflows.ts`
- Create: `apps/web/src/data/connectors.ts`
- Create: `apps/web/src/data/runs.ts`
- Create: `apps/web/src/data/dashboard.ts`

- [ ] **Step 1: Create workflows.ts**

`apps/web/src/data/workflows.ts`:
```typescript
import type { WorkflowSummary, WorkflowDefinition } from '@/types'

export const workflowSummaries: WorkflowSummary[] = [
  {
    workflowId: 'wf_001',
    name: 'Salesforce → BigQuery Sync',
    status: 'published',
    lastRunAt: '2026-04-19T08:00:00Z',
    lastRunStatus: 'success',
    updatedAt: '2026-04-18T14:30:00Z',
    trigger: { type: 'schedule', cron: '0 8 * * *' },
  },
  {
    workflowId: 'wf_002',
    name: 'Stripe Payments Pipeline',
    status: 'published',
    lastRunAt: '2026-04-19T06:30:00Z',
    lastRunStatus: 'failed',
    updatedAt: '2026-04-17T10:00:00Z',
    trigger: { type: 'webhook' },
  },
  {
    workflowId: 'wf_003',
    name: 'HubSpot Contact Enrichment',
    status: 'draft',
    updatedAt: '2026-04-19T11:00:00Z',
    trigger: { type: 'manual' },
  },
  {
    workflowId: 'wf_004',
    name: 'Postgres → Snowflake Replication',
    status: 'published',
    lastRunAt: '2026-04-19T09:15:00Z',
    lastRunStatus: 'success',
    updatedAt: '2026-04-15T09:00:00Z',
    trigger: { type: 'schedule', cron: '*/15 * * * *' },
  },
]

export const workflowDefinitions: WorkflowDefinition[] = [
  {
    workflowId: 'wf_001',
    version: 3,
    status: 'published',
    name: 'Salesforce → BigQuery Sync',
    description: 'Syncs Salesforce contacts to BigQuery daily.',
    trigger: { type: 'schedule', cron: '0 8 * * *' },
    nodes: [
      {
        id: 'source_1',
        type: 'connector.source',
        label: 'Salesforce',
        config: { object: 'Contact', fields: ['Id', 'Name', 'Email', 'AccountId'] },
        connectorId: 'conn_salesforce',
      },
      {
        id: 'transform_1',
        type: 'transform.map',
        label: 'Map Fields',
        config: { mappings: { id: 'Id', name: 'Name', email: 'Email' } },
      },
      {
        id: 'dest_1',
        type: 'connector.destination',
        label: 'BigQuery',
        config: { dataset: 'crm', table: 'contacts', writeDisposition: 'WRITE_TRUNCATE' },
        connectorId: 'conn_bigquery',
      },
    ],
    edges: [
      { id: 'e1', source: 'source_1', target: 'transform_1' },
      { id: 'e2', source: 'transform_1', target: 'dest_1' },
    ],
  },
  {
    workflowId: 'wf_002',
    version: 1,
    status: 'published',
    name: 'Stripe Payments Pipeline',
    description: 'Routes Stripe payment events to Postgres or Slack based on status.',
    trigger: { type: 'webhook' },
    nodes: [
      {
        id: 'source_1',
        type: 'connector.source',
        label: 'Stripe Webhook',
        config: { events: ['payment_intent.succeeded', 'payment_intent.failed'] },
        connectorId: 'conn_stripe',
      },
      {
        id: 'logic_1',
        type: 'logic.branch',
        label: 'Route by Status',
        config: { condition: 'event.type === "payment_intent.succeeded"' },
      },
      {
        id: 'dest_success',
        type: 'connector.destination',
        label: 'Postgres',
        config: { table: 'successful_payments' },
        connectorId: 'conn_postgres',
      },
      {
        id: 'dest_failed',
        type: 'connector.destination',
        label: 'Slack Alert',
        config: { channel: '#payments-alerts' },
        connectorId: 'conn_slack',
      },
    ],
    edges: [
      { id: 'e1', source: 'source_1', target: 'logic_1' },
      { id: 'e2', source: 'logic_1', target: 'dest_success' },
      { id: 'e3', source: 'logic_1', target: 'dest_failed' },
    ],
  },
  {
    workflowId: 'wf_004',
    version: 2,
    status: 'published',
    name: 'Postgres → Snowflake Replication',
    description: 'Incremental replication from Postgres to Snowflake every 15 minutes.',
    trigger: { type: 'schedule', cron: '*/15 * * * *' },
    nodes: [
      {
        id: 'source_1',
        type: 'connector.source',
        label: 'Postgres',
        config: { table: 'orders', incrementalKey: 'updated_at' },
        connectorId: 'conn_postgres',
      },
      {
        id: 'dest_1',
        type: 'connector.destination',
        label: 'Snowflake',
        config: { database: 'PROD', schema: 'PUBLIC', table: 'ORDERS' },
        connectorId: 'conn_snowflake',
      },
    ],
    edges: [
      { id: 'e1', source: 'source_1', target: 'dest_1' },
    ],
  },
]
```

- [ ] **Step 2: Create connectors.ts**

`apps/web/src/data/connectors.ts`:
```typescript
import type { ConnectorCard, ConnectorDetail } from '@/types'

export const connectorCards: ConnectorCard[] = [
  {
    connectorId: 'conn_salesforce',
    name: 'Salesforce',
    description: 'CRM data sync and automation',
    category: 'saas',
    authMethod: 'oauth2',
    logoInitial: 'SF',
    certified: true,
    installed: true,
  },
  {
    connectorId: 'conn_bigquery',
    name: 'BigQuery',
    description: 'Google Cloud data warehouse',
    category: 'database',
    authMethod: 'oauth2',
    logoInitial: 'BQ',
    certified: true,
    installed: true,
  },
  {
    connectorId: 'conn_stripe',
    name: 'Stripe',
    description: 'Payment processing and billing events',
    category: 'saas',
    authMethod: 'api-key',
    logoInitial: 'ST',
    certified: true,
    installed: false,
  },
  {
    connectorId: 'conn_postgres',
    name: 'PostgreSQL',
    description: 'Open source relational database',
    category: 'database',
    authMethod: 'basic',
    logoInitial: 'PG',
    certified: true,
    installed: true,
  },
  {
    connectorId: 'conn_hubspot',
    name: 'HubSpot',
    description: 'Marketing and CRM platform',
    category: 'saas',
    authMethod: 'oauth2',
    logoInitial: 'HS',
    certified: true,
    installed: false,
  },
  {
    connectorId: 'conn_slack',
    name: 'Slack',
    description: 'Send messages and notifications',
    category: 'messaging',
    authMethod: 'oauth2',
    logoInitial: 'SL',
    certified: true,
    installed: false,
  },
  {
    connectorId: 'conn_s3',
    name: 'Amazon S3',
    description: 'Cloud object storage for files and logs',
    category: 'storage',
    authMethod: 'api-key',
    logoInitial: 'S3',
    certified: true,
    installed: false,
  },
  {
    connectorId: 'conn_snowflake',
    name: 'Snowflake',
    description: 'Cloud data platform and warehouse',
    category: 'database',
    authMethod: 'basic',
    logoInitial: 'SN',
    certified: true,
    installed: false,
  },
]

export const connectorDetails: ConnectorDetail[] = [
  {
    connectorId: 'conn_salesforce',
    name: 'Salesforce',
    description: 'CRM data sync and automation',
    category: 'saas',
    authMethod: 'oauth2',
    logoInitial: 'SF',
    certified: true,
    installed: true,
    version: '2.4.1',
    actions: [
      {
        actionId: 'read_objects',
        name: 'Read Objects',
        description: 'Extract records from any Salesforce object',
      },
      {
        actionId: 'write_objects',
        name: 'Write Objects',
        description: 'Insert or upsert records into Salesforce',
      },
      {
        actionId: 'describe_schema',
        name: 'Discover Schema',
        description: 'Fetch field definitions for any Salesforce object',
      },
    ],
    authRequirements: ['Client ID', 'Client Secret', 'Instance URL (production or sandbox)'],
    docsUrl: 'https://docs.example.com/connectors/salesforce',
  },
  {
    connectorId: 'conn_postgres',
    name: 'PostgreSQL',
    description: 'Open source relational database',
    category: 'database',
    authMethod: 'basic',
    logoInitial: 'PG',
    certified: true,
    installed: true,
    version: '1.8.0',
    actions: [
      {
        actionId: 'read_table',
        name: 'Read Table',
        description: 'Extract rows from a table with optional incremental key',
      },
      {
        actionId: 'write_table',
        name: 'Write Table',
        description: 'Insert or upsert rows into a table',
      },
      {
        actionId: 'run_query',
        name: 'Run Query',
        description: 'Execute a custom SQL query',
      },
    ],
    authRequirements: ['Host', 'Port', 'Database', 'Username', 'Password'],
    docsUrl: 'https://docs.example.com/connectors/postgres',
  },
]
```

- [ ] **Step 3: Create runs.ts**

`apps/web/src/data/runs.ts`:
```typescript
import type { RunRecord, RunDetail } from '@/types'

export const runRecords: RunRecord[] = [
  {
    runId: 'run_001',
    workflowId: 'wf_001',
    workflowName: 'Salesforce → BigQuery Sync',
    status: 'success',
    triggeredBy: 'schedule',
    startedAt: '2026-04-19T08:00:00Z',
    completedAt: '2026-04-19T08:02:31Z',
    durationMs: 151000,
    nodeCount: 3,
    failedNodeCount: 0,
  },
  {
    runId: 'run_002',
    workflowId: 'wf_002',
    workflowName: 'Stripe Payments Pipeline',
    status: 'failed',
    triggeredBy: 'webhook',
    startedAt: '2026-04-19T06:30:00Z',
    completedAt: '2026-04-19T06:30:04Z',
    durationMs: 4200,
    nodeCount: 4,
    failedNodeCount: 1,
  },
  {
    runId: 'run_003',
    workflowId: 'wf_004',
    workflowName: 'Postgres → Snowflake Replication',
    status: 'success',
    triggeredBy: 'schedule',
    startedAt: '2026-04-19T09:15:00Z',
    completedAt: '2026-04-19T09:15:48Z',
    durationMs: 48000,
    nodeCount: 2,
    failedNodeCount: 0,
  },
]

export const runDetails: RunDetail[] = [
  {
    runId: 'run_002',
    workflowId: 'wf_002',
    workflowName: 'Stripe Payments Pipeline',
    status: 'failed',
    triggeredBy: 'webhook',
    startedAt: '2026-04-19T06:30:00Z',
    completedAt: '2026-04-19T06:30:04Z',
    durationMs: 4200,
    nodeCount: 4,
    failedNodeCount: 1,
    nodes: [
      {
        nodeId: 'source_1',
        nodeLabel: 'Stripe Webhook',
        status: 'success',
        startedAt: '2026-04-19T06:30:00.000Z',
        completedAt: '2026-04-19T06:30:00.400Z',
        durationMs: 400,
        logs: [
          {
            timestamp: '2026-04-19T06:30:00.100Z',
            level: 'info',
            message: 'Received webhook event: payment_intent.succeeded',
          },
          {
            timestamp: '2026-04-19T06:30:00.400Z',
            level: 'info',
            message: 'Payload validated, 1 event extracted',
          },
        ],
      },
      {
        nodeId: 'logic_1',
        nodeLabel: 'Route by Status',
        status: 'success',
        startedAt: '2026-04-19T06:30:00.400Z',
        completedAt: '2026-04-19T06:30:00.500Z',
        durationMs: 100,
        logs: [
          {
            timestamp: '2026-04-19T06:30:00.500Z',
            level: 'info',
            message: 'Condition matched: routing to success branch',
          },
        ],
      },
      {
        nodeId: 'dest_success',
        nodeLabel: 'Postgres',
        status: 'failed',
        startedAt: '2026-04-19T06:30:00.500Z',
        completedAt: '2026-04-19T06:30:04.000Z',
        durationMs: 3500,
        logs: [
          {
            timestamp: '2026-04-19T06:30:00.500Z',
            level: 'info',
            message: 'Connecting to Postgres at db.internal:5432...',
          },
          {
            timestamp: '2026-04-19T06:30:02.000Z',
            level: 'warn',
            message: 'Retry 1/3 — connection timeout after 1500ms',
          },
          {
            timestamp: '2026-04-19T06:30:03.000Z',
            level: 'warn',
            message: 'Retry 2/3 — connection timeout after 1000ms',
          },
          {
            timestamp: '2026-04-19T06:30:04.000Z',
            level: 'error',
            message: 'Failed after 3 retries: connection refused on port 5432',
          },
        ],
      },
    ],
  },
]
```

- [ ] **Step 4: Create dashboard.ts**

`apps/web/src/data/dashboard.ts`:
```typescript
import type { DashboardStats } from '@/types'
import { runRecords } from './runs'

export const dashboardStats: DashboardStats = {
  totalWorkflows: 4,
  activeWorkflows: 3,
  totalRunsToday: 12,
  failedRunsToday: 1,
  successRate: 91.7,
  connectorCount: 8,
  recentRuns: runRecords,
  failedRuns: runRecords.filter((r) => r.status === 'failed'),
}
```

- [ ] **Step 5: Verify TypeScript compiles all data files**

```bash
cd apps/web && npm run build
```

Expected: build succeeds with no type errors.

- [ ] **Step 6: Commit**

```bash
git add apps/web/src/data apps/web/src/types
git commit -m "feat: add typed stub data files for workflows, connectors, runs, dashboard"
```

---

## Task 7: Workflows Pinia store and tests

**Files:**
- Create: `apps/web/src/stores/workflows.ts`
- Create: `apps/web/src/stores/__tests__/workflows.test.ts`

- [ ] **Step 1: Write the failing tests first**

`apps/web/src/stores/__tests__/workflows.test.ts`:
```typescript
import { describe, it, expect, beforeEach } from 'vitest'
import { setActivePinia, createPinia } from 'pinia'
import { useWorkflowsStore } from '../workflows'

describe('useWorkflowsStore', () => {
  beforeEach(() => {
    setActivePinia(createPinia())
  })

  it('exposes workflow summaries from stub data', () => {
    const store = useWorkflowsStore()
    expect(store.summaries.length).toBeGreaterThan(0)
  })

  it('counts published workflows via computed', () => {
    const store = useWorkflowsStore()
    const expected = store.summaries.filter((w) => w.status === 'published').length
    expect(store.publishedCount).toBe(expected)
  })

  it('finds a workflow definition by id', () => {
    const store = useWorkflowsStore()
    const def = store.getDefinition('wf_001')
    expect(def).toBeDefined()
    expect(def?.workflowId).toBe('wf_001')
    expect(def?.nodes.length).toBeGreaterThan(0)
    expect(def?.edges.length).toBeGreaterThan(0)
  })

  it('returns undefined for an unknown workflow id', () => {
    const store = useWorkflowsStore()
    expect(store.getDefinition('nonexistent')).toBeUndefined()
  })

  it('finds a workflow summary by id', () => {
    const store = useWorkflowsStore()
    const summary = store.getSummary('wf_001')
    expect(summary?.name).toBe('Salesforce → BigQuery Sync')
  })

  it('returns undefined summary for an unknown id', () => {
    const store = useWorkflowsStore()
    expect(store.getSummary('nonexistent')).toBeUndefined()
  })
})
```

- [ ] **Step 2: Run tests to confirm they fail**

```bash
cd apps/web && npm test
```

Expected: 6 tests fail with `Cannot find module '../workflows'`.

- [ ] **Step 3: Implement the store**

`apps/web/src/stores/workflows.ts`:
```typescript
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import { workflowSummaries, workflowDefinitions } from '@/data/workflows'
import type { WorkflowSummary, WorkflowDefinition } from '@/types'

export const useWorkflowsStore = defineStore('workflows', () => {
  const summaries = ref<WorkflowSummary[]>(workflowSummaries)
  const definitions = ref<WorkflowDefinition[]>(workflowDefinitions)

  const publishedCount = computed(
    () => summaries.value.filter((w) => w.status === 'published').length,
  )

  function getDefinition(workflowId: string): WorkflowDefinition | undefined {
    return definitions.value.find((d) => d.workflowId === workflowId)
  }

  function getSummary(workflowId: string): WorkflowSummary | undefined {
    return summaries.value.find((s) => s.workflowId === workflowId)
  }

  return { summaries, definitions, publishedCount, getDefinition, getSummary }
})
```

- [ ] **Step 4: Run tests to confirm they pass**

```bash
npm test
```

Expected:
```
✓ src/stores/__tests__/workflows.test.ts (6)
Test Files  1 passed (1)
Tests       6 passed (6)
```

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/stores
git commit -m "feat: add workflows Pinia store with stub data"
```

---

## Task 8: Connectors Pinia store and tests

**Files:**
- Create: `apps/web/src/stores/connectors.ts`
- Create: `apps/web/src/stores/__tests__/connectors.test.ts`

- [ ] **Step 1: Write the failing tests**

`apps/web/src/stores/__tests__/connectors.test.ts`:
```typescript
import { describe, it, expect, beforeEach } from 'vitest'
import { setActivePinia, createPinia } from 'pinia'
import { useConnectorsStore } from '../connectors'

describe('useConnectorsStore', () => {
  beforeEach(() => {
    setActivePinia(createPinia())
  })

  it('exposes connector cards from stub data', () => {
    const store = useConnectorsStore()
    expect(store.cards.length).toBeGreaterThan(0)
  })

  it('counts installed connectors via computed', () => {
    const store = useConnectorsStore()
    const expected = store.cards.filter((c) => c.installed).length
    expect(store.installedCount).toBe(expected)
  })

  it('returns a connector detail by id', () => {
    const store = useConnectorsStore()
    const detail = store.getDetail('conn_salesforce')
    expect(detail).toBeDefined()
    expect(detail?.actions.length).toBeGreaterThan(0)
  })

  it('returns undefined for an unknown connector id', () => {
    const store = useConnectorsStore()
    expect(store.getDetail('nonexistent')).toBeUndefined()
  })

  it('filters connectors by category', () => {
    const store = useConnectorsStore()
    const databases = store.filterByCategory('database')
    expect(databases.length).toBeGreaterThan(0)
    expect(databases.every((c) => c.category === 'database')).toBe(true)
  })

  it('returns all connectors when category is empty string', () => {
    const store = useConnectorsStore()
    expect(store.filterByCategory('').length).toBe(store.cards.length)
  })

  it('searches connectors by name case-insensitively', () => {
    const store = useConnectorsStore()
    expect(store.search('SALESFORCE').length).toBeGreaterThan(0)
    expect(store.search('salesforce')[0].name).toBe('Salesforce')
  })

  it('searches connectors by description', () => {
    const store = useConnectorsStore()
    const results = store.search('payment')
    expect(results.some((c) => c.name === 'Stripe')).toBe(true)
  })

  it('returns empty array for no search match', () => {
    const store = useConnectorsStore()
    expect(store.search('zzznomatch999').length).toBe(0)
  })
})
```

- [ ] **Step 2: Run tests to confirm they fail**

```bash
npm test
```

Expected: 9 new tests fail with `Cannot find module '../connectors'`.

- [ ] **Step 3: Implement the store**

`apps/web/src/stores/connectors.ts`:
```typescript
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import { connectorCards, connectorDetails } from '@/data/connectors'
import type { ConnectorCard, ConnectorDetail } from '@/types'

export const useConnectorsStore = defineStore('connectors', () => {
  const cards = ref<ConnectorCard[]>(connectorCards)
  const details = ref<ConnectorDetail[]>(connectorDetails)

  const installedCount = computed(
    () => cards.value.filter((c) => c.installed).length,
  )

  function getDetail(connectorId: string): ConnectorDetail | undefined {
    return details.value.find((d) => d.connectorId === connectorId)
  }

  function filterByCategory(category: string): ConnectorCard[] {
    if (!category) return cards.value
    return cards.value.filter((c) => c.category === category)
  }

  function search(query: string): ConnectorCard[] {
    const q = query.toLowerCase()
    return cards.value.filter(
      (c) =>
        c.name.toLowerCase().includes(q) ||
        c.description.toLowerCase().includes(q),
    )
  }

  return { cards, details, installedCount, getDetail, filterByCategory, search }
})
```

- [ ] **Step 4: Run tests to confirm all pass**

```bash
npm test
```

Expected:
```
✓ src/stores/__tests__/connectors.test.ts (9)
✓ src/stores/__tests__/workflows.test.ts (6)
Test Files  2 passed (2)
Tests       15 passed (15)
```

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/stores
git commit -m "feat: add connectors Pinia store with search and filter"
```

---

## Task 9: Runs Pinia store and tests

**Files:**
- Create: `apps/web/src/stores/runs.ts`
- Create: `apps/web/src/stores/__tests__/runs.test.ts`

- [ ] **Step 1: Write the failing tests**

`apps/web/src/stores/__tests__/runs.test.ts`:
```typescript
import { describe, it, expect, beforeEach } from 'vitest'
import { setActivePinia, createPinia } from 'pinia'
import { useRunsStore } from '../runs'

describe('useRunsStore', () => {
  beforeEach(() => {
    setActivePinia(createPinia())
  })

  it('exposes run records from stub data', () => {
    const store = useRunsStore()
    expect(store.records.length).toBeGreaterThan(0)
  })

  it('returns a run detail by id', () => {
    const store = useRunsStore()
    const detail = store.getDetail('run_002')
    expect(detail).toBeDefined()
    expect(detail?.nodes.length).toBeGreaterThan(0)
  })

  it('returns undefined for an unknown run id', () => {
    const store = useRunsStore()
    expect(store.getDetail('nonexistent')).toBeUndefined()
  })

  it('returns runs filtered by workflow id', () => {
    const store = useRunsStore()
    const runs = store.getByWorkflow('wf_002')
    expect(runs.length).toBeGreaterThan(0)
    expect(runs.every((r) => r.workflowId === 'wf_002')).toBe(true)
  })

  it('returns empty array when no runs match the workflow id', () => {
    const store = useRunsStore()
    expect(store.getByWorkflow('nonexistent').length).toBe(0)
  })
})
```

- [ ] **Step 2: Run tests to confirm they fail**

```bash
npm test
```

Expected: 5 new tests fail with `Cannot find module '../runs'`.

- [ ] **Step 3: Implement the store**

`apps/web/src/stores/runs.ts`:
```typescript
import { defineStore } from 'pinia'
import { ref } from 'vue'
import { runRecords, runDetails } from '@/data/runs'
import type { RunRecord, RunDetail } from '@/types'

export const useRunsStore = defineStore('runs', () => {
  const records = ref<RunRecord[]>(runRecords)
  const details = ref<RunDetail[]>(runDetails)

  function getDetail(runId: string): RunDetail | undefined {
    return details.value.find((d) => d.runId === runId)
  }

  function getByWorkflow(workflowId: string): RunRecord[] {
    return records.value.filter((r) => r.workflowId === workflowId)
  }

  return { records, details, getDetail, getByWorkflow }
})
```

- [ ] **Step 4: Run tests to confirm all 20 pass**

```bash
npm test
```

Expected:
```
✓ src/stores/__tests__/runs.test.ts (5)
✓ src/stores/__tests__/connectors.test.ts (9)
✓ src/stores/__tests__/workflows.test.ts (6)
Test Files  3 passed (3)
Tests       20 passed (20)
```

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/stores
git commit -m "feat: add runs Pinia store; all 20 store tests passing"
```

---

## Task 10: Configure Vue Router with placeholder views

**Files:**
- Create: `apps/web/src/router/index.ts`
- Create: `apps/web/src/views/DashboardView.vue`
- Create: `apps/web/src/views/WorkflowsView.vue`
- Create: `apps/web/src/views/WorkflowBuilderView.vue`
- Create: `apps/web/src/views/ConnectorsView.vue`
- Create: `apps/web/src/views/ConnectorDetailView.vue`
- Create: `apps/web/src/views/RunDetailView.vue`

- [ ] **Step 1: Create the router**

`apps/web/src/router/index.ts`:
```typescript
import { createRouter, createWebHistory } from 'vue-router'

export const router = createRouter({
  history: createWebHistory(import.meta.env.BASE_URL),
  routes: [
    { path: '/', redirect: '/dashboard' },
    {
      path: '/dashboard',
      name: 'dashboard',
      component: () => import('@/views/DashboardView.vue'),
    },
    {
      path: '/workflows',
      name: 'workflows',
      component: () => import('@/views/WorkflowsView.vue'),
    },
    {
      path: '/workflows/:id/edit',
      name: 'workflow-builder',
      component: () => import('@/views/WorkflowBuilderView.vue'),
    },
    {
      path: '/connectors',
      name: 'connectors',
      component: () => import('@/views/ConnectorsView.vue'),
    },
    {
      path: '/connectors/:id',
      name: 'connector-detail',
      component: () => import('@/views/ConnectorDetailView.vue'),
    },
    {
      path: '/runs/:id',
      name: 'run-detail',
      component: () => import('@/views/RunDetailView.vue'),
    },
  ],
})
```

- [ ] **Step 2: Create DashboardView.vue**

`apps/web/src/views/DashboardView.vue`:
```vue
<template>
  <div>
    <h1 class="text-2xl font-semibold tracking-tight">Dashboard</h1>
    <p class="text-muted-foreground mt-1 text-sm">Overview of platform health and activity.</p>
  </div>
</template>
```

- [ ] **Step 3: Create WorkflowsView.vue**

`apps/web/src/views/WorkflowsView.vue`:
```vue
<template>
  <div>
    <h1 class="text-2xl font-semibold tracking-tight">Workflows</h1>
    <p class="text-muted-foreground mt-1 text-sm">Design and manage your integration workflows.</p>
  </div>
</template>
```

- [ ] **Step 4: Create WorkflowBuilderView.vue**

`apps/web/src/views/WorkflowBuilderView.vue`:
```vue
<script setup lang="ts">
import { useRoute } from 'vue-router'

const route = useRoute()
</script>

<template>
  <div>
    <h1 class="text-2xl font-semibold tracking-tight">Workflow Builder</h1>
    <p class="text-muted-foreground mt-1 text-sm">Editing workflow {{ route.params.id }}</p>
  </div>
</template>
```

- [ ] **Step 5: Create ConnectorsView.vue**

`apps/web/src/views/ConnectorsView.vue`:
```vue
<template>
  <div>
    <h1 class="text-2xl font-semibold tracking-tight">Connectors</h1>
    <p class="text-muted-foreground mt-1 text-sm">Browse and install connectors.</p>
  </div>
</template>
```

- [ ] **Step 6: Create ConnectorDetailView.vue**

`apps/web/src/views/ConnectorDetailView.vue`:
```vue
<script setup lang="ts">
import { useRoute } from 'vue-router'

const route = useRoute()
</script>

<template>
  <div>
    <h1 class="text-2xl font-semibold tracking-tight">Connector Detail</h1>
    <p class="text-muted-foreground mt-1 text-sm">Connector: {{ route.params.id }}</p>
  </div>
</template>
```

- [ ] **Step 7: Create RunDetailView.vue**

`apps/web/src/views/RunDetailView.vue`:
```vue
<script setup lang="ts">
import { useRoute } from 'vue-router'

const route = useRoute()
</script>

<template>
  <div>
    <h1 class="text-2xl font-semibold tracking-tight">Run Inspector</h1>
    <p class="text-muted-foreground mt-1 text-sm">Run: {{ route.params.id }}</p>
  </div>
</template>
```

- [ ] **Step 8: Run all tests to confirm nothing broke**

```bash
cd apps/web && npm test
```

Expected: 20 tests still pass.

- [ ] **Step 9: Commit**

```bash
git add apps/web/src/router apps/web/src/views
git commit -m "feat: configure Vue Router with 6 routes and placeholder views"
```

---

## Task 11: Build AppSidebar and tests

**Files:**
- Create: `apps/web/src/components/layout/AppSidebar.vue`
- Create: `apps/web/src/components/layout/__tests__/AppSidebar.test.ts`

- [ ] **Step 1: Write the failing tests**

`apps/web/src/components/layout/__tests__/AppSidebar.test.ts`:
```typescript
import { describe, it, expect } from 'vitest'
import { mount } from '@vue/test-utils'
import { createRouter, createMemoryHistory } from 'vue-router'
import AppSidebar from '../AppSidebar.vue'

function makeRouter(initialPath = '/dashboard') {
  const router = createRouter({
    history: createMemoryHistory(),
    routes: [
      { path: '/dashboard', component: { template: '<div />' } },
      { path: '/workflows', component: { template: '<div />' } },
      { path: '/connectors', component: { template: '<div />' } },
      { path: '/monitoring', component: { template: '<div />' } },
      { path: '/settings', component: { template: '<div />' } },
    ],
  })
  router.push(initialPath)
  return router
}

describe('AppSidebar', () => {
  it('renders the brand name', async () => {
    const router = makeRouter()
    await router.isReady()
    const wrapper = mount(AppSidebar, { global: { plugins: [router] } })
    expect(wrapper.text()).toContain('vipsOS')
  })

  it('renders all 5 navigation items', async () => {
    const router = makeRouter()
    await router.isReady()
    const wrapper = mount(AppSidebar, { global: { plugins: [router] } })
    expect(wrapper.text()).toContain('Dashboard')
    expect(wrapper.text()).toContain('Workflows')
    expect(wrapper.text()).toContain('Connectors')
    expect(wrapper.text()).toContain('Monitoring')
    expect(wrapper.text()).toContain('Settings')
  })

  it('applies active class to the current route link', async () => {
    const router = makeRouter('/workflows')
    await router.isReady()
    const wrapper = mount(AppSidebar, { global: { plugins: [router] } })
    const links = wrapper.findAll('a')
    const workflowsLink = links.find((l) => l.text().includes('Workflows'))
    const dashboardLink = links.find((l) => l.text().includes('Dashboard'))
    expect(workflowsLink?.classes()).toContain('text-primary')
    expect(dashboardLink?.classes()).not.toContain('text-primary')
  })

  it('does not mark dashboard active when on workflows route', async () => {
    const router = makeRouter('/workflows')
    await router.isReady()
    const wrapper = mount(AppSidebar, { global: { plugins: [router] } })
    const dashboardLink = wrapper.findAll('a').find((l) => l.text().includes('Dashboard'))
    expect(dashboardLink?.classes()).toContain('text-muted-foreground')
  })
})
```

- [ ] **Step 2: Run tests to confirm they fail**

```bash
npm test
```

Expected: 4 new AppSidebar tests fail with `Cannot find module '../AppSidebar.vue'`.

- [ ] **Step 3: Implement AppSidebar.vue**

`apps/web/src/components/layout/AppSidebar.vue`:
```vue
<script setup lang="ts">
import { RouterLink, useRoute } from 'vue-router'
import {
  LayoutDashboard,
  GitBranch,
  Plug,
  Activity,
  Settings,
} from 'lucide-vue-next'
import type { Component } from 'vue'

interface NavItem {
  label: string
  to: string
  icon: Component
}

const route = useRoute()

const navItems: NavItem[] = [
  { label: 'Dashboard', to: '/dashboard', icon: LayoutDashboard },
  { label: 'Workflows', to: '/workflows', icon: GitBranch },
  { label: 'Connectors', to: '/connectors', icon: Plug },
  { label: 'Monitoring', to: '/monitoring', icon: Activity },
  { label: 'Settings', to: '/settings', icon: Settings },
]

function isActive(to: string): boolean {
  return route.path === to || route.path.startsWith(to + '/')
}
</script>

<template>
  <aside class="w-60 flex-shrink-0 border-r bg-background flex flex-col">
    <div class="h-14 flex items-center px-5 border-b">
      <span class="font-semibold text-sm tracking-tight">vipsOS</span>
    </div>
    <nav class="flex-1 px-2 py-3 space-y-0.5">
      <RouterLink
        v-for="item in navItems"
        :key="item.to"
        :to="item.to"
        class="flex items-center gap-3 px-3 py-2 rounded-md text-sm transition-colors"
        :class="
          isActive(item.to)
            ? 'bg-primary/10 text-primary font-medium'
            : 'text-muted-foreground hover:bg-muted hover:text-foreground'
        "
      >
        <component :is="item.icon" class="h-4 w-4 flex-shrink-0" />
        {{ item.label }}
      </RouterLink>
    </nav>
  </aside>
</template>
```

- [ ] **Step 4: Run tests to confirm all 24 pass**

```bash
npm test
```

Expected:
```
✓ src/components/layout/__tests__/AppSidebar.test.ts (4)
✓ src/stores/__tests__/runs.test.ts (5)
✓ src/stores/__tests__/connectors.test.ts (9)
✓ src/stores/__tests__/workflows.test.ts (6)
Test Files  4 passed (4)
Tests       24 passed (24)
```

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/components
git commit -m "feat: add AppSidebar with active route highlighting"
```

---

## Task 12: Build AppTopBar

**Files:**
- Create: `apps/web/src/components/layout/AppTopBar.vue`

- [ ] **Step 1: Create AppTopBar.vue**

`apps/web/src/components/layout/AppTopBar.vue`:
```vue
<script setup lang="ts">
import { ref } from 'vue'
import { ChevronDown, Bell, User } from 'lucide-vue-next'

const workspaces = ['Acme Corp', 'Demo Workspace', 'Personal']
const currentWorkspace = ref(workspaces[0])
</script>

<template>
  <header
    class="h-14 flex-shrink-0 border-b bg-background flex items-center justify-between px-6"
  >
    <button
      class="flex items-center gap-1.5 text-sm font-medium hover:text-muted-foreground transition-colors"
    >
      {{ currentWorkspace }}
      <ChevronDown class="h-3.5 w-3.5 text-muted-foreground" />
    </button>
    <div class="flex items-center gap-1">
      <button
        class="p-2 rounded-md text-muted-foreground hover:text-foreground hover:bg-muted transition-colors"
        aria-label="Notifications"
      >
        <Bell class="h-4 w-4" />
      </button>
      <button
        class="h-8 w-8 rounded-full bg-muted flex items-center justify-center text-muted-foreground hover:bg-muted/70 transition-colors"
        aria-label="User menu"
      >
        <User class="h-4 w-4" />
      </button>
    </div>
  </header>
</template>
```

- [ ] **Step 2: Run tests to confirm still 24 passing**

```bash
cd apps/web && npm test
```

Expected: 24 tests pass.

- [ ] **Step 3: Commit**

```bash
git add apps/web/src/components/layout/AppTopBar.vue
git commit -m "feat: add AppTopBar with workspace switcher and user actions"
```

---

## Task 13: Build AppShell, wire App.vue and main.ts, final verification

**Files:**
- Create: `apps/web/src/components/layout/AppShell.vue`
- Modify: `apps/web/src/App.vue`
- Modify: `apps/web/src/main.ts`

- [ ] **Step 1: Create AppShell.vue**

`apps/web/src/components/layout/AppShell.vue`:
```vue
<script setup lang="ts">
import { RouterView } from 'vue-router'
import AppSidebar from './AppSidebar.vue'
import AppTopBar from './AppTopBar.vue'
</script>

<template>
  <div class="flex h-screen overflow-hidden bg-background text-foreground">
    <AppSidebar />
    <div class="flex flex-col flex-1 overflow-hidden">
      <AppTopBar />
      <main class="flex-1 overflow-y-auto p-6">
        <RouterView />
      </main>
    </div>
  </div>
</template>
```

- [ ] **Step 2: Update App.vue to render AppShell only**

`apps/web/src/App.vue`:
```vue
<script setup lang="ts">
import AppShell from '@/components/layout/AppShell.vue'
</script>

<template>
  <AppShell />
</template>
```

- [ ] **Step 3: Update main.ts to bootstrap Pinia + Router**

`apps/web/src/main.ts`:
```typescript
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import './style.css'
import App from './App.vue'
import { router } from './router'

const app = createApp(App)
app.use(createPinia())
app.use(router)
app.mount('#app')
```

- [ ] **Step 4: Run all tests**

```bash
cd apps/web && npm test
```

Expected: 24 tests pass.

- [ ] **Step 5: Start the dev server and verify in browser**

```bash
npm run dev
```

Open http://localhost:5173 and verify:

1. The app shell renders — sidebar on the left, topbar across the top, content area on the right
2. The sidebar shows: vipsOS brand, Dashboard, Workflows, Connectors, Monitoring, Settings
3. The topbar shows: "Acme Corp" workspace switcher, bell icon, user icon
4. Dashboard is highlighted in the sidebar (active state) — it has a light background tint
5. Click **Workflows** → URL changes to `/workflows`, "Workflows" gets highlighted, "Dashboard" loses highlight
6. Click **Connectors** → URL changes to `/connectors`, correct item highlighted
7. Click browser back button → returns to previous route, active state updates correctly
8. No errors in the browser console

- [ ] **Step 6: Stop the server**

- [ ] **Step 7: Commit**

```bash
git add apps/web/src
git commit -m "feat: wire AppShell into App.vue with Pinia + Router bootstrap — Week 1 complete"
```

---

## Week 1 Complete — Verification Checklist

Run through this before moving to Week 2:

- [ ] `npm test` in `apps/web` shows 24 passing tests, 0 failing
- [ ] `npm run build` succeeds with no TypeScript errors
- [ ] `npm run dev` starts without errors
- [ ] App shell renders correctly in the browser
- [ ] All 5 nav items are visible and clicking them changes the route
- [ ] Active nav item is visually highlighted
- [ ] Browser console shows no errors
